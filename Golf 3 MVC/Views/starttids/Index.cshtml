@model IEnumerable<Golf_3_MVC.Models.starttid>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.starttid1 }) |
            @Html.ActionLink("Details", "Details", new { id=item.starttid1 }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.starttid1 })
        </td>
    </tr>
}

</table>
